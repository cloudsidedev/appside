#
# --- THIS FILE IS AUTOMATICALLY PROVISIONED THROUGH ANSIBLE ---
#

APP_ENV={{ conf_env }}

APP_DEBUG=false

# Laravel 5
DB_DATABASE={{ item.value.db_name }}
DB_USERNAME={{ item.value.db_user }}

# Laravel <= 4.2
DB_NAME={{ item.value.db_name }}
DB_USER={{ item.value.db_user }}
DB_PASSWORD={{ item.value.db_password }}
DB_HOST={{ item.value.db_server }}

{% if item.value.env_opt is defined %}
{{ item.value.env_opt }}{% endif %}
