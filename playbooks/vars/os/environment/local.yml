---

# file: playbooks/vars/os/distribution/MacOSX.yml

#
# Base
#
conf_hold_packages:
    - mod-pagespeed-stable
conf_unwanted_packages:
    # - chef
conf_base_packages:
    #- assh
    - zsh
    - vim
    # - screen
    - go
    - tmux
    - tmate
    - mosh
    - tree
    - unzip
    - whois
    - htop
    - ccze
    - subversion
    - nmap
    - wget
    - curl
    - links
    - lynx
    - traceroute
    - make
    # - "{{ var_package_git }}"
    - convmv

